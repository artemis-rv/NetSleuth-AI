# Place your local PCAP/PCAPNG evidence files here for development testing.
# This directory is intentionally empty in the repository.
#
# DO NOT commit evidence files — they may contain sensitive network data.
# Add *.pcap and *.pcapng to .gitignore if not already present.
#
# Example usage:
#   from backend.app.engines.acquisition import AcquisitionService
#   ref = AcquisitionService().acquire("sample_data/evidence/your_capture.pcap")
