[project]
name = "netsleuth-ai"
version = "0.1.0"
description = "NetSleuth-AI: Advanced network and security analysis project."
authors = [
    {name = "NetSleuth Team"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jsonschema>=4.20.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
