"""
backend/app/api/v1/reports.py
-----------------------------
Reports API Router boundary (APP-0 structural placeholder).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/reports", tags=["Reports"])
