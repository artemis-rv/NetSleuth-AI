"""
backend/app/api/v1/auth.py
--------------------------
Authentication API Router boundary (APP-0 structural placeholder).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/auth", tags=["Authentication"])
