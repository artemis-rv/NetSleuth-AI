"""
backend/app/api/v1/acquisitions.py
----------------------------------
Acquisitions API Router boundary (APP-0 structural placeholder).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/acquisitions", tags=["Acquisitions"])
